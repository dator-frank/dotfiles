# Virtual Sink/Source for ReplaySOrcery
context.modules = [
    { factory = adapter
        args = {
            factory.name    = support.null-audio-sink
            node.name       = "ReplaySorcery Virtual Source"
            media.class     = Audio/Source/Virtual
            object.linger   = true
            audio.position  = [ FL FR ]
        }
    }
]
